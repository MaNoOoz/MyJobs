<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.manooz.jobs_search_engine_material">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--<meta-data-->
            <!--android:name="com.google.android.gms.version"-->
            <!--android:value="@integer/google_play_services_version" />-->

        <!--<meta-data-->
            <!--android:name="com.google.android.geo.API_KEY"-->
            <!--android:value="@string/google_maps_key" />-->

        <activity
            android:name="com.manooz.myjobs.MainActivity"
            android:label="@string/app_name_short"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.manooz.myjobs.StartActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme3.NoActionBar"
            android:windowSoftInputMode="stateHidden">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <!-- <activity -->
        <!-- android:name=".UI.Chat_Activity" -->
        <!-- android:label="@string/app_name_short" -->
        <!-- android:screenOrientation="portrait" -->
        <!-- android:theme="@style/AppTheme"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <activity
            android:name="com.manooz.myjobs.RegisterActivity"
            android:parentActivityName="com.manooz.myjobs.StartActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme3.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name="com.manooz.myjobs.loginActivity"
            android:parentActivityName="com.manooz.myjobs.StartActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme3.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <!-- <activity -->
        <!-- android:name=".Main.SettingActivity" -->
        <!-- android:screenOrientation="portrait" -->
        <!-- android:theme="@style/AppTheme3.NoActionBar" -->
        <!-- android:windowSoftInputMode="stateHidden" /> -->
        <!--<activity-->
            <!--android:name="com.theartofdev.edmodo.cropper.CropImageActivity"-->
            <!--android:theme="@style/Base.Theme.AppCompat" /> &lt;!&ndash; optional (needed if default theme has no action bar) &ndash;&gt;-->
        <activity
            android:name="com.manooz.myjobs.AllUsers"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme3.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name="com.manooz.myjobs.about"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme4.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <!--<meta-data-->
            <!--android:name="com.google.android.geo.API_KEY"-->
            <!--android:value="@string/google_maps_key" />-->


    </application>

</manifest>